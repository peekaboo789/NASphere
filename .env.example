# 复制成 .env 再用（deploy.sh 读它）：

#   mkdir -p ./dat && cp .env.example ./dat/.env && vi ./dat/.env

# 只对 deploy.sh 有效：脚本每次从「安装目录」里读这份文件（<安装目录>/.env），
# 放在别处不会生效。生效顺序是 命令行 > 环境变量 > .env > 默认值。
#
# .env 已经在 .gitignore 与 .dockerignore 里，不会被提交、也不会进镜像。
#
# 手工 docker compose 的路线不读这个文件：docker-compose.yml 里镜像、端口、卷都是写死的值，
# 要改就改那个文件本身。

# 宿主机对外端口。容器内部固定监听 18086（镜像里 ENV PORT=18086），只挪这一边就够了。
HOST_PORT=18086

# 镜像与标签。默认就是官方发布在 GHCR 上的那份；标签默认 1.0.4。
# 换成自己 build 的镜像就改 IMAGE，例如 local/nasphere。
# IMAGE=local/nasphere
# TAG=1.0.4

# 数据目录（配置、账号、壁纸、图标都在这里）。默认 <安装目录>/data。
# DATA_DIR=/volume1/docker/nasphere/data

# 安装目录。命令行 --root 优先；相对路径按执行命令时所在目录解释，默认 ./dat。
# INSTALL_ROOT=./dat

# 时区，只影响容器内的时间显示（日志、天气的当天分界）。
# TZ=Asia/Shanghai

# Docker Socket 路径。找不到这个路径时脚本会跳过挂载，主页的容器组件就没数据。
# DOCKER_SOCK=/var/run/docker.sock

# 逐卷用量。这一行留空（默认）时脚本会自己探这台 NAS 的存储池（/vol1、/volume1、/storage1、/data1、/mnt/* 这几类摆法），
# 探到的每一卷各挂成 /host/<同名>:ro，主页「设置 → 应用矩阵 → NAS 资源」于是看得见全部卷，点一下加一张读数卡；
# NAS 上加了卷要重跑一次 ./deploy.sh，那一列才会跟着变。
# 想点名单就写成空格分隔的目录（目录名只能用字母、数字、点、减号、下划线，那串字符就是卡片认卷的 id，路径中间不能有空格）；
# 只要数据目录那一卷、别的都别挂进来，就写 HOST_VOLUMES=none。
# ⚠️ 挂进来的那一卷，容器进程技术上就能读它的内容了；页面本身只报数字，不接受这条代价就写 none。
# HOST_VOLUMES=/volume1 /volume2 /volume3
# HOST_VOLUMES=none

# 起容器后最多等多少秒探活（探的是免登录的 /api/health）。
# HEALTH_WAIT=40

# 每次部署前给 config.json / auth.json 留几份快照（data/.deploy-backup/）。
# KEEP_BACKUPS=5

# 登录账号与密码不在这里配。首启就是镜像内置的 admin / admin123，
# 装完立刻登录去「设置 → 安全」改掉，之后一律以 data/auth.json 为准。
# （服务端确实有 NAV_USER / NAV_PASSWORD 这两个环境变量，但脚本走 compose 起容器，
# 不会替你注入；要自己配的话在生成的 compose 里加 environment 行。）
